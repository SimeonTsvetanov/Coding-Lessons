function solution(params) {
    // MasK
    let target = Number(params.shift());
    let result = 0;

    while ('I say so!') {
        result += Number(params.shift());
        if (result >= target) {
            console.log(result);
            break;
        }
    }
}

solution(["100",
"10",
"20",
"30",
"40"])

// 100

console.log('********************************');

solution(["20",
"1",
"2",
"3",
"4",
"5",
"6"])

// 21