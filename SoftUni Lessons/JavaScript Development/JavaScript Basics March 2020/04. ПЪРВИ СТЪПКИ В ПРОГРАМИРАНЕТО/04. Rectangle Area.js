function ractangeArea() {
    // Having no need to add input just yet.
    // even if we print something extra stuped. 
    // It will still work :D
    let a = 2;
    let b = 7;

    let area = a * b;
    console.log(area);
}

ractangeArea();
