function mask (text, index, count) {
    // MASK
    console.log(text.substr(index, count));
}

mask('ASentence', 1, 8);
// Sentence

console.log('-------------------------------------');

mask('SkipWord', 4, 7);
// Word
