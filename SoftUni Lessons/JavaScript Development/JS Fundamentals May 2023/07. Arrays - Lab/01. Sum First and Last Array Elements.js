function mask (nums) {
    // MASK
    const first = nums[0];
    const last = nums[nums.length - 1];

    console.log(first + last);
}

mask([20, 30, 40]);
// 60

mask([10, 17, 22, 33] );
// 43
