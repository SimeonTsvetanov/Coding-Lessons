function reverseAnArrayOfString(array) {
    // Mask
    console.log(array.reverse().join(' '));
}

reverseAnArrayOfString(['a', 'b', 'c', 'd', 'e']);  // Should return: e d c b a

reverseAnArrayOfString(['abc', 'def', 'hig', 'klm', 'nop']);  // Should return: dd 0 123 33