function smallestOfThreeNumbers(one, two, three) {
    // Mask
    console.log(Math.min(one, two, three));
}

smallestOfThreeNumbers(2,5,3);  // Should return: 2

smallestOfThreeNumbers(1,17,30);  // Should return: 1