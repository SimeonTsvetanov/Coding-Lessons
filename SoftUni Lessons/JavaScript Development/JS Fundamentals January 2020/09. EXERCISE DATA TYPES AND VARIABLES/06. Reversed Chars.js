function reversedChars(one, two, three) {
    // Mask
    console.log(`${three} ${two} ${one}`);
}

reversedChars('A', 'B', 'C');  // Should return: C B A

reversedChars('1', 'L', '&');  // Should return: & L 1