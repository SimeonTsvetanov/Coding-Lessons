function biggestOfThreeNumbers(one, two, three) {
    // Mask
    console.log(Math.max(one, two, three));
}

biggestOfThreeNumbers(-2, 7, 3);  // Should return: 7

biggestOfThreeNumbers(130, 5, 99);  // Should return: 130