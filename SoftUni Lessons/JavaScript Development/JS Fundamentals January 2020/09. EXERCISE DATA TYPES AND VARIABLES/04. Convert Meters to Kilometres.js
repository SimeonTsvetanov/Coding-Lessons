function metersToKilometers(meters) {
    // Mask
    console.log((meters / 1000).toFixed(2));
}

metersToKilometers(1852);  // Should return: 1.80

metersToKilometers(798);  // Should return: 0.80