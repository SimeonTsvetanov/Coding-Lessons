function charsToString(one, two, three) {
    // Mask
    console.log(`${one}${two}${three}`);
}

charsToString('a', 'b', 'c');  // Should return: abc

charsToString('%', '2', 'o');  // Should return: %2o